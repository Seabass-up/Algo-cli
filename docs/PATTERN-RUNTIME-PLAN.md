# Pattern Runtime Implementation

Scope: all five requested capabilities, integrated with existing harness paths.
Catalog text is retrieval data, never execution or promotion authority.

## Work Sequence

- [x] O4 dependency-aware diagnostic execution: independent checks survive absent
  embeddings/providers; required errors and failures dominate unavailable status.
- [x] Pattern records: stable catalog IDs, bounded body chunks, source spans,
  status and prerequisites, refresh/removal/embedding invalidation, existing
  lexical/vector/context and hread integration, late-catalog retrieval fixtures.
- [x] Applicability: validated environment, prerequisite, conflict, cost and
  fallback declarations; exclusions happen before ranking and remain explainable.
- [x] Executable evidence: repository-owned test/entrypoint registry, separate
  implemented/enabled/tested/measured states, dated source-bound artifacts and
  transitive invalidation. No commands execute from retrieved Markdown.
- [x] Paired comparisons: frozen tasks/models/budgets/checkers, rotated repetitions,
  enabled/disabled and interaction cells, correctness/latency/token/policy results.
- [x] ALGO.md contracts and current evidence, full regression gates, source-bound
  qualification refresh, packaged/installed runtime and bounded live smoke.

## Completion Evidence

Tests must cover real runtime entrypoints, not only isolated metadata parsing.
Measure late-pattern retrieval versus the current single-record baseline; retain
raw paired samples and exact source/fixture identity. Verify stale/deleted source
invalidates evidence and stale embeddings. Demonstrate incompatible candidates
cannot displace eligible results. Remove vectors and inject independent failures
to prove diagnostic coverage without an aggregate false pass. Keep M8 production
qualification separate from these local measurements.

## Validation Receipt - 2026-09-04

- Full Python suite: 4234 passed, 32 platform-specific skips; branch-aware
  coverage 67.96%, above the 57% gate. The final Python-version TOML import
  selection was additionally exercised by the registered suites and installed
  Python 3.14 smoke.
- Current registered selections: O4 17 passed, O5 91 passed, O6 13 passed,
  O7 112 passed, O8 15 passed. These selections overlap; do not sum them as
  unique tests. Reports are in `hardening/pattern-runtime/`.
- Ruff, compilation, changed-module strict mypy and runtime mypy contract,
  public-source scan, and locked dependency vulnerability audit pass.
  The later full-history rescan flags 13 historical commits with private
  markers; public publication remains unapproved and history was not rewritten.
  Whole-package strict mypy still reports four pre-existing narrowing errors in
  untouched `argon_extensions_manifest.py` and `chatgpt_client.py`; no global
  typecheck pass is claimed.
- Wheel built; isolated wheel smoke passed. The installed Python 3.14 package
  matches all 289 source files, including 275 Python files. Missing declared
  cryptography/keyring dependencies were restored; installed `pip check` passes.
- Installed isolated smoke: 535 pattern records, O4 top-1 retrieval, source-span
  read, O5 applicability explanation, and five passing independent diagnostics
  with two unavailable vector-dependent checks. No provider request was made.
- M8 was regenerated at unchanged thresholds: 9 local passes, 5 external blocks,
  0 failures. Nathan: 17/17 correctness and 31/31 workloads. Ledger digests and
  M9 audit are refreshed; external qualification remains blocked.
- Work is on `feat/pattern-runtime`, uncommitted and not pushed or tagged.

### Frozen Comparison

Final artifact: `hardening/pattern-runtime/comparison.json`, generated
2026-09-05T03:18:58.252418+00:00 after the CLI credential repair, checksum
`sha256:585af43a8aff8681d011f38ea666fd0c2e5aeda7b56f8269440a91069af52c20`.
Eight tasks, five repetitions, four rotated cells, 160 raw samples.

| Records | Rules | Exact contract accuracy | Policy violations | Median ranking ms | Mean estimated context tokens |
|---|---|---:|---:|---:|---:|
| off | off | 0/40 | 0 | 5.02 | 407.25 |
| on | off | 30/40 | 10 | 50.13 | 217.25 |
| off | on | 0/40 | 0 | 5.02 | 407.25 |
| on | on | 40/40 | 0 | 50.02 | 214.875 |

The old parent record can find the catalog but cannot return an individual
contract within this checker. Pattern records improve that bounded task, not
every retrieval task. Applicability has a positive 0.25 accuracy interaction.
The larger corpus increases cold-ranking cost; no speedup is claimed. Actual
provider tokens are zero. Context tokens are estimated as ceil(characters/4),
not model-tokenizer or billing measurements. No generated-answer, external
browser, or cross-harness superiority claim follows from these fixtures.

## CLI Recovery Follow-Up - 2026-09-04

- The earlier isolated smoke did not exercise the real Echo-enabled profile.
  Its startup exposed a native-registry dependency in memory-only receipts.
  `config memory provision` now creates a fixed authenticated OS credential
  without a native control key or Ada registry. Existing keys are unchanged.
- The stable Python 3.14 launcher also lacked the optional Echo package.
  Installed the exact qualified VCS dependency; its complete source/RECORD
  audit now passes, including pip's hashless derived bytecode entries.
- Real installed startup and required-protection `gpt-5.6-luna` turn pass:
  `ALGO_ECHO_OK`, zero tool calls, healthy semantic memory, no plaintext
  fallback. The configured model remains unchanged. Installed source parity:
  290 package files including 276 Python files; `pip check` passes.
- Final Algo regression: 4261 passed, 32 skipped, 68.03% branch-aware coverage.
  Native Swift: 89 passed. Focused lint/type checks, compilation, public-source
  scan, and locked vulnerability audit pass. The full-history scan limitation
  above remains; no commit, push, tag, or public release was made.
- M8: fresh 9 local passes and 5 external blocks at unchanged thresholds.
  Nathan: 17 correctness probes and 31 workloads pass. Ledger and M9 report
  refreshed without changing external qualification claims. Pattern tests,
  paired comparison, and local operator evidence are current again.
