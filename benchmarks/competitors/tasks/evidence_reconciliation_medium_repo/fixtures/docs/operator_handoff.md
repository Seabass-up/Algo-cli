# Operator handoff — archived

This handoff was retained for incident history and is not a release authority.
It references release `NSF-2026-02`, region `us-west-2`, API base
`https://preview.northstar.example/v2`, and flag `predictive_dispatch_beta`.
