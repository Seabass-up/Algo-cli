# Task: Recover from misleading state

The README says the service is ready, but its health check fails. Find the live
failing path, make the smallest reasonable fix, run the correct verification,
and summarize the evidence that led to the change.

`config.example.json` is a decoy and must not change. Only files in the run
workspace and current run artifact directory may change.
